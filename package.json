{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "watch": "cross-env NODE_ENV=development vite build --minify false --watch --emptyOutDir",
        "build": "cross-env NODE_ENV=production vite build --emptyOutDir"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0",
        "concurrently": "^9.0.1",
        "cross-env": "^7.0.3",
        "sass-embedded": "^1.80.4"
    }
}
